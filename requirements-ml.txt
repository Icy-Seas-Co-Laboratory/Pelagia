# Optional machine-learning dependencies for U-Net ROI refinement adapters.
# Install this in addition to requirements.txt only on machines that will run
# learned refinement models.
-r requirements.txt
-e ".[ml]"
