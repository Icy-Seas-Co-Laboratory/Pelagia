# Built-In Plugin Manifests

This directory reserves the packaged plugin namespace. Pelagia does not execute
or dynamically load plugins yet; for now, plugin support is manifest discovery
only.

Each future plugin should live in its own directory with a `metadata.toml` file.
