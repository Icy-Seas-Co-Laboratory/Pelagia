# Pelagia Assets

Packaged assets are files that ship with Pelagia and can be discovered without
runtime installation steps. This tree is for small manifests, schemas, built-in
plugins. Executable model artifacts belong in Oracle Builder.

Large or user-imported artifacts should usually live in the configured local
artifact library, which defaults to `./.pelagia/`.
